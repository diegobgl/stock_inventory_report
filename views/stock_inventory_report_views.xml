<odoo>
    <!-- Vista tipo tree para el reporte de inventario -->
    <record id="view_stock_inventory_report_tree" model="ir.ui.view">
        <field name="name">stock.inventory.report.tree</field>
        <field name="model">stock.inventory.report</field>
        <field name="arch" type="xml">
            <tree string="Reporte de Inventario">
                <field name="product_id"/>
                <field name="location_id"/>
                <field name="quantity"/>
                <field name="lot_id"/>
                <field name="date"/>
                <field name="move_type"/> <!-- Tipo de movimiento -->
                <field name="unit_value"/> <!-- Valor unitario -->
                <field name="total_value"/> <!-- Valor total -->
            </tree>
        </field>
    </record>

    <!-- Acción para abrir la vista del reporte -->
    <record id="action_stock_inventory_report" model="ir.actions.act_window">
        <field name="name">Reporte de Inventario</field>
        <field name="res_model">stock.inventory.report</field>
        <field name="view_mode">tree</field>
        <field name="target">main</field>
    </record>
</odoo>
